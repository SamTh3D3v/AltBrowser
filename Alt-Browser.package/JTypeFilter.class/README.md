RVTypeFilter filters variables and values based on their type; it contains a target type and remove all the items which do not belong to it.

Instance Variables:
	type	<Object>	The type to filter the trace tree against

